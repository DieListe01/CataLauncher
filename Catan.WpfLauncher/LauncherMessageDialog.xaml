<Window x:Class="Catan.WpfLauncher.LauncherMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hinweis"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="Transparent">
    <Border Background="{StaticResource Brush.WindowGradient}" BorderBrush="#A97542" BorderThickness="1" CornerRadius="18">
        <Grid>
            <Image Source="Assets/catan-die-erste-insel-backround.jpg" Stretch="UniformToFill" Opacity="0.035" />
            <Rectangle Fill="#38FFF7EA" RadiusX="18" RadiusY="18" />

            <Grid Margin="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Cursor="SizeAll" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="18" Height="18" CornerRadius="5" Background="#D84D2B" BorderBrush="#7A2C17" BorderThickness="1">
                            <TextBlock Text="!" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" />
                        </Border>
                        <TextBlock x:Name="WindowCaptionText" Text="Launcher Hinweis" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#5A3F28" FontSize="13" FontFamily="Bahnschrift SemiBold" />
                    </StackPanel>

                    <Button Grid.Column="1" Click="CloseButton_Click" Style="{StaticResource WindowChromeButtonStyle}" Background="#E8C7B5" BorderBrush="#B57E65" Content="X" />
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,12,0,0">
                    <StackPanel>
                        <WrapPanel>
                            <Border Style="{StaticResource StatusBadgeStyle}" Background="{StaticResource Brush.BadgeWarning}" BorderBrush="#C8923D" BorderThickness="1">
                                <TextBlock Text="Achtung" Foreground="#7A531F" FontFamily="Bahnschrift SemiBold" FontSize="11" />
                            </Border>
                        </WrapPanel>

                        <TextBlock x:Name="DialogTitleText" Margin="0,6,0,0" Foreground="{StaticResource Brush.Text}" FontFamily="Bahnschrift SemiBold" FontSize="24" TextWrapping="Wrap" />
                        <TextBlock x:Name="DialogMessageText" Margin="0,10,0,0" Foreground="{StaticResource Brush.TextMuted}" FontSize="13" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                    <Button Width="132" Style="{StaticResource PrimaryButtonStyle}" Content="Alles klar" Click="OkButton_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
